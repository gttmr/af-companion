# Copy this file to runtime.env. runtime.env is gitignored.
# Use this as the single local source for shared Agent Factory runtime secrets.
GOOGLE_API_KEY=

# Optional: base URL of the Mock Lab network MCP endpoint for connected adapters.
AF_MOCK_LAB_MCP_URL=http://127.0.0.1:5173/api/mock-lab/mcp
