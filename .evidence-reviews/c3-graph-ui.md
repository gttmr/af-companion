## Dead or flag-parked (INSPECTOR_ENABLED map with LOC for delete-vs-revive)

Review boundary: findings only. No source files were edited, and no build/dev server was run per task. Evidence is source inspection plus `rg`, `nl`, `awk`, `wc`, and one read-only `node` enum-coverage script.

- `INSPECTOR_ENABLED` is a compile-time false flag: `packages/web/src/routes/design/designStageModelCore.ts:8` exports `INSPECTOR_ENABLED = false`, `packages/web/src/routes/design/designStageModel.tsx:1-10` re-exports it, and `packages/web/src/routes/design/DesignGraphPanel.tsx:13` imports it.
- The flag changes the design grid class at `packages/web/src/routes/design/DesignGraphPanel.tsx:112` and gates only the right-side `<aside className="af-design-inspector">` branch at `packages/web/src/routes/design/DesignGraphPanel.tsx:176-207`.
- The disabled right pane contains three render paths:
  - runtime tab: `RuntimeContractInspector` at `packages/web/src/routes/design/DesignGraphPanel.tsx:178-179`.
  - A2A tab: `A2AContractInspector` at `packages/web/src/routes/design/DesignGraphPanel.tsx:180-187`.
  - other tabs: `SelectionHeader` plus `CommentThread` at `packages/web/src/routes/design/DesignGraphPanel.tsx:189-204`.
- `GraphInspector` is not parked by this flag. It is live in the left sidebar when edit mode is not active at `packages/web/src/routes/design/DesignGraphPanel.tsx:120-139`, and it is also live inside standalone `GraphCanvas` when `hideInspector` is false at `packages/web/src/components/GraphCanvas.tsx:544-556`. In Design review, `GraphCanvas` passes `hideInspector` at `packages/web/src/routes/design/DesignGraphPanel.tsx:155-165`, so only the internal canvas inspector is suppressed there.
- A2A contract editing is not parked. The bottom A2A tab mounts `A2AContractSidebar` and `A2AContractInspector` at `packages/web/src/routes/design/DesignA2ATab.tsx:48-56`, and the bottom tab itself is mounted by `packages/web/src/routes/design/DesignBottomPanel.tsx:134-145`.
- Anchored comments are not parked. The bottom review-notes panel mounts `CommentThread` at `packages/web/src/design/ReviewNotesPanel.tsx:64-77`, and `commentAnchorFromSelection` is covered for node, edge, both, and empty selections at `packages/web/src/design/reviewNotesModel.test.ts:62-78`.
- Runtime contract list/review status is live, but runtime contract editing is parked. The bottom runtime tab mounts only `RuntimeContractSidebar` at `packages/web/src/routes/design/DesignBottomPanel.tsx:131-133`; `RuntimeContractInspector`/`RuntimeContractEditor` are defined at `packages/web/src/design/RuntimeContractPanel.tsx:88-277`, and `rg -n "RuntimeContractInspector|RuntimeContractEditor" packages/web/src` found render usage only in the disabled right pane at `packages/web/src/routes/design/DesignGraphPanel.tsx:179`.
- `SelectionHeader` appears to be deleteable if the right pane is deleted permanently. `rg -n "SelectionHeader" packages/web/src` found only its definition at `packages/web/src/routes/design/DesignSelectionHeader.tsx:5` and the disabled branch import/render at `packages/web/src/routes/design/DesignGraphPanel.tsx:14,190`.
- CSS contains dedicated flag parking rules: three-column grid at `packages/web/src/styles/router/design.css:74-79`, no-inspector two-column rule/comment at `packages/web/src/styles/router/design.css:81-86`, shared surface selector including `.af-design-inspector` at `packages/web/src/styles/router/design.css:94-102`, and inspector-only padding/scroll at `packages/web/src/styles/router/design.css:436-439`.
- LOC evidence:
  - `awk 'NR>=176 && NR<=207 && $0 !~ /^[[:space:]]*$/ {c++} END {print c}' packages/web/src/routes/design/DesignGraphPanel.tsx` -> 32 nonblank lines in the disabled right-pane render branch.
  - `awk 'NR>=88 && NR<=277 && $0 !~ /^[[:space:]]*$/ {c++} END {print c}' packages/web/src/design/RuntimeContractPanel.tsx` -> 172 nonblank lines for `RuntimeContractInspector` plus `RuntimeContractEditor`.
  - `wc -l packages/web/src/routes/design/DesignSelectionHeader.tsx` -> 36 lines.
  - `awk` over `packages/web/src/styles/router/design.css:81-86` -> 6 nonblank no-inspector lines; `awk` over `packages/web/src/styles/router/design.css:436-439` -> 4 nonblank inspector-only lines.
- HYPOTHESIS, delete-permanent option: if the product decision is to remove the right inspector permanently, a lower-bound delete is about 300 LOC: 32 branch lines + 172 parked runtime editor lines + 36 `SelectionHeader` lines + 10 CSS lines + about 45 branch-only prop/call-site lines visible in `DesignGraphPanel` props/destructure (`packages/web/src/routes/design/DesignGraphPanel.tsx:25,34-35,40-43,51-58,63,72-73,78-81,89-96`) and call-site props (`packages/web/src/routes/design/DesignReviewStep.tsx:117,126-127,132-135,143-150`). This does not delete `GraphInspector`, `A2AContractInspector`, or `CommentThread`, because they have live paths cited above.
- HYPOTHESIS, revive option: if the product decision is to revive the right inspector, the behavior-preserving cleanup is smaller, roughly 10-15 LOC, by deleting the false flag/re-export/import/conditional class plus the no-inspector CSS rule/comment while retaining the right-pane branch. Runtime editing would become visible again, but `RuntimeContractInspector` currently uses a cast into `RuntimeContractEditor` at `packages/web/src/design/RuntimeContractPanel.tsx:105-107`, so reviving should include a small type cleanup.
- Separate non-flag dead branch: `isNodeRuntimeControlEditable` always returns false at `packages/web/src/components/graphElementEditorModel.ts:57-59`, tests assert false for agent, adapter_call, and input at `packages/web/src/components/graphElementEditorModel.test.ts:163-165`, yet `GraphElementEditor` still imports `GRAPH_INVOKE_BINDINGS` at `packages/web/src/components/GraphElementEditor.tsx:4-17`, defines `INVOKE_BINDING_LABEL` at `packages/web/src/components/GraphElementEditor.tsx:71-83`, computes `runtimeControlEditable` at `packages/web/src/components/GraphElementEditor.tsx:385-389`, and renders an editable `invoke_binding` select only behind that never-true predicate at `packages/web/src/components/GraphElementEditor.tsx:570-586`. That is a behavior-preserving delete target unless the runtime-control editor is intentionally revived.

## Duplication

- Node-kind to module-category mapping is duplicated in three scoped components:
  - `GraphInspector` has `moduleCatFromKind` at `packages/web/src/components/GraphInspector.tsx:32-40`.
  - node cards have `moduleCategoryFromKind` at `packages/web/src/components/graph/nodeTypes.tsx:6-14`.
  - editor module matching has `moduleCategoryFromNodeKind` at `packages/web/src/components/GraphElementEditor.tsx:897-903`.
  All three encode the same `workflow_call -> workflow`, `adapter_call -> adapter`, `remote_agent_call -> remote_a2a`, direct-category, else-null mapping.
- Graph element tabs are duplicated:
  - `GraphInspector` defines `GraphElementTabs` at `packages/web/src/components/GraphInspector.tsx:100-125`.
  - `GraphElementEditor` defines the same role/class/button structure at `packages/web/src/components/GraphElementEditor.tsx:185-210`.
  The only behavioral difference is the `aria-label` text, so a shared local component with an `ariaLabel` prop would keep behavior.
- Edge ID fallback logic is repeated in multiple paths:
  - `DesignWorkbench` builds `edge.id ?? \`edge-${index}\`` at `packages/web/src/routes/DesignWorkbench.tsx:112-114`.
  - `GraphCanvas` builds the same map at `packages/web/src/components/GraphCanvas.tsx:427-431` and has `edgeKey` at `packages/web/src/components/GraphCanvas.tsx:1118-1120`.
  - `GraphElementEditor` computes selected edge IDs at `packages/web/src/components/GraphElementEditor.tsx:998-1000`.
  - graph layout assigns ReactFlow edge IDs at `packages/web/src/components/graph/layout.ts:282-288`.
  This is not a deletion target by itself, but centralizing the helper would reduce drift risk around optional `GraphEdge.id`.
- Router route-map presentation is duplicated in two visual densities:
  - Inspector table: `RouteMapTable` at `packages/web/src/components/GraphInspector.tsx:81-98`, rendered at `packages/web/src/components/GraphInspector.tsx:254`.
  - Node card route map: `packages/web/src/components/graph/nodeTypes.tsx:137-170`.
  Both consume the same `GraphRouteSummary` shape from `packages/web/src/components/graph/layout.ts:24-30`; a shared renderer is possible, though the card/table density difference may justify keeping separate presentation.
- A2A and runtime contract editors repeat the same draft/save/revert/readiness structure:
  - A2A editor draft/dirty/block/save/revert at `packages/web/src/design/A2AContractPanel.tsx:149-223`.
  - runtime editor draft/dirty/block/save/revert at `packages/web/src/design/RuntimeContractPanel.tsx:117-155`.
  This is a low-priority simplification only if both editors remain live; if runtime editing is deleted, do not abstract it.

## Over-abstraction & slop

- Size hotspot command evidence:

```text
packages/web/src/components/GraphCanvas.tsx 1131
packages/web/src/components/GraphElementEditor.tsx 916
packages/web/src/components/GraphInspector.tsx 436
packages/web/src/design/A2AContractPanel.tsx 769
packages/web/src/design/RuntimeContractPanel.tsx 259
packages/web/src/components/graph/layout.ts 299
packages/web/src/components/graph/nodeTypes.tsx 229
packages/web/src/components/graph/edgeTypes.tsx 191
packages/web/src/components/graph/containerOverlay.tsx 55
packages/web/src/components/graph/validationBanner.tsx 65
packages/web/src/graph/containerMembership.ts 47
packages/web/src/components/graphElementEditorModel.ts 149
```

  Command was `for f in ...; do awk 'NF && $0 !~ /^[[:space:]]*\\/\\//' "$f" | wc -l; done`, so counts are nonblank/non-comment line counts.
- `GraphCanvas.tsx` carries too many layers in one file: props/types at `packages/web/src/components/GraphCanvas.tsx:40-108`, state and draft setup at `packages/web/src/components/GraphCanvas.tsx:128-153`, draft mutation callbacks starting at `packages/web/src/components/GraphCanvas.tsx:155-180`, layout/collaboration/select derivation at `packages/web/src/components/GraphCanvas.tsx:373-458`, rendering at `packages/web/src/components/GraphCanvas.tsx:471-558`, toolbar at `packages/web/src/components/GraphCanvas.tsx:561-716`, ReactFlow stage at `packages/web/src/components/GraphCanvas.tsx:718-853`, graph mutation helpers at `packages/web/src/components/GraphCanvas.tsx:855-1121`, and collaboration mark construction at `packages/web/src/components/GraphCanvas.tsx:1122-1221`.
- `GraphCanvas` already has pure-ish helper seams suitable for extraction without behavior change: `buildEditableNode`, `buildEditableEdge`, `deleteFromGraph`, `applyNodeFields`, `applyEdgeFields`, `applyCurrentPositions`, and `edgeKey` at `packages/web/src/components/GraphCanvas.tsx:879-1120`; collaboration marks are isolated at `packages/web/src/components/GraphCanvas.tsx:1122-1221`.
- `GraphElementEditor.tsx` mixes constants, tabs, field subcomponents, node form, edge form, and JSON parsers: labels/constants at `packages/web/src/components/GraphElementEditor.tsx:41-122`, tabs at `packages/web/src/components/GraphElementEditor.tsx:185-210`, node form starts at `packages/web/src/components/GraphElementEditor.tsx:366`, edge selection uses `selectedEdgeId` at `packages/web/src/components/GraphElementEditor.tsx:998-1000`, and parser/formatter helpers occupy `packages/web/src/components/GraphElementEditor.tsx:905-991`.
- `A2AContractPanel.tsx` is a design/graph-adjacent hotspot: the inspector/editor entry is at `packages/web/src/design/A2AContractPanel.tsx:99-139`, the main editor starts at `packages/web/src/design/A2AContractPanel.tsx:149`, nested update functions run through `packages/web/src/design/A2AContractPanel.tsx:158-213`, the action row ends at `packages/web/src/design/A2AContractPanel.tsx:510-518`, and section editors/utilities continue through `packages/web/src/design/A2AContractPanel.tsx:522-810`.
- `A2AContractPanel` uses `JSON.stringify(draft) !== JSON.stringify(contract)` for dirty detection at `packages/web/src/design/A2AContractPanel.tsx:154-155`. This is acceptable for current data size, but it is slop compared with the field-level dirty checks already used by runtime contracts at `packages/web/src/design/RuntimeContractPanel.tsx:129-132`.
- `RuntimeContractInspector` has a type cast that is only needed because null handling and editor props are coupled: `packages/web/src/design/RuntimeContractPanel.tsx:95-107`. If revived, this should be simplified before more fields are added.
- Tests are mostly model/layout-level, not component render-level. `rg -n "GraphCanvas|GraphInspector|GraphElementEditor|nodeTypes|edgeTypes|RuntimeContractInspector|A2AContractInspector" packages/web/src --glob '*test.*'` found only model/layout references: layout persistence/route tests at `packages/web/src/components/graph/layout.test.ts:78-148,179-198` and editor-model tests at `packages/web/src/components/graphElementEditorModel.test.ts:147-169`. No render tests cover the parked right pane, `GraphInspector`, node cards, edge cards, or contract inspectors.

## Design-system drift (badge/token/glyph/CSS-pitfall)

- `subtypeGlyph` covers every requested analyzer enum. Code evidence: `SubtypeGlyphKey` includes `AdapterKind | AgentKind | WorkflowKind | RemoteContractKind | RuntimeContractKind` at `packages/web/src/components/CategoryBadge.tsx:21`, `subtypeGlyph` is declared at `packages/web/src/components/CategoryBadge.tsx:30-51` with `satisfies Record<SubtypeGlyphKey, string>`, and analyzer enums are in `packages/web/src/analyzer/types.ts:3-23`. Command evidence:

```text
agentKinds: values=specialist,shared missing=(none)
workflowKinds: values=orchestration,graph,dynamic,unknown missing=(none)
adapterKinds: values=legacy_api,retrieval,rule_registry,data_query,template,computation,external_service,unknown missing=(none)
remoteContractKinds: values=a2a,unknown missing=(none)
```

- Docs drift: the design-system glyph prose says subtype coverage is `workflow_kind / adapter_kind / agent_kind / remote_contract_kind` at `docs/visualization/design-system.md:137-142`, but code also treats runtime contract kinds as subtype glyph keys at `packages/web/src/components/CategoryBadge.tsx:21,41-46`; runtime kinds are declared at `packages/web/src/analyzer/types.ts:101-108` and labeled at `packages/web/src/analyzer/classificationRules.ts:56-63`.
- CategoryBadge rendering is mostly compliant in scoped graph code: `GraphInspector` imports and renders `CategoryBadge`/`SubtypeBadge` at `packages/web/src/components/GraphInspector.tsx:10,201-202`, and graph node cards do the same at `packages/web/src/components/graph/nodeTypes.tsx:2,64-65`.
- Category documentation is inconsistent with the `CategoryBadge` API. Docs list category glyphs for `input` and `output` at `docs/visualization/design-system.md:129-135`, but `CategoryBadge` exports `categoryGlyph: Record<ModuleCategory, string>` with only `agent`, `workflow`, `adapter`, and `remote_a2a` at `packages/web/src/components/CategoryBadge.tsx:23-28`; `moduleCategories` has the same four values at `packages/web/src/analyzer/types.ts:1`. `input`/`output` are tokenized visual lane colors, not module categories, with tokens at `packages/web/src/styles/tokens.css:71-76`.
- Node-card subtype drift: docs say Graph node headers should show category/subtype, label, module id, and review status, while runtime binding/invoke/call-control/Mock Lab/ADK details stay off the card at `docs/visualization/design-system.md:237-242`; docs specifically say legacy/technical `execution_kind` labels such as `llm_single_turn`, `single_turn`, `chat`, and `task` should not repeat as an agent header subtype at `docs/visualization/design-system.md:241`. `nodeTypes.tsx` currently computes `sub` directly from arbitrary `graphNode.execution_kind` at `packages/web/src/components/graph/nodeTypes.tsx:50-65`, and `GraphNode.execution_kind` is typed as `string | null` at `packages/web/src/analyzer/types.ts:870-876`. Real fixtures include free legacy values such as `llm_agent`, `human_review`, and `request_input` at `templates/regression-scenarios/scenario-d-graph-workflow/analysis-result.json:545-577`, plus `llm_agent` in an active artifact at `artifacts/af/req-page-recommendation-required/process-flow.json:98-104`. Since `SubtypeBadge` falls back to a dot for unknown strings at `packages/web/src/components/CategoryBadge.tsx:95-101`, this can render legacy labels as subtype-looking badges.
- Inspector also repeats raw `execution_kind` while showing agent mode. Docs say Inspector follows the same de-duplication rule and raw execution kind should not be repeated in the main info area at `docs/visualization/design-system.md:245-249`; `GraphInspector` renders `execution_kind` and `agent mode` adjacent in the runtime group at `packages/web/src/components/GraphInspector.tsx:293-296`. HYPOTHESIS: this is acceptable only if runtime group is considered raw/debug detail; otherwise it violates the visual-owner rule.
- Token contract drift: docs say all tokens live in `tokens.css` and other stylesheets must not hard-code colors, font sizes, spacing, or radii at `docs/visualization/design-system.md:94-99`, and `tokens.css` says the same at `packages/web/src/styles/tokens.css:1-5`. `category.css` claims it consumes tokens only at `packages/web/src/styles/category.css:1-10`, but contains raw colors for subtype/protocol/affinity styles at `packages/web/src/styles/category.css:86-96,99-117,197-212`.
- More token drift in graph code: `GraphCanvas` highlight colors use token fallbacks with raw hex values at `packages/web/src/components/GraphCanvas.tsx:1133-1139`; `edgeTypes` uses raw fallback hexes and an rgba drop-shadow in edge styles at `packages/web/src/components/graph/edgeTypes.tsx:13-97,141-161`. Docs allow a few intentional transparent effects, but fallback hexes duplicate token defaults and are not necessary if tokens are loaded (`docs/visualization/design-system.md:123`).
- CSS pitfall check: exact search command `rg -n "td\\s+span|td\\s+\\.category|td\\s+\\.subtype|table\\s+span|table\\s+\\.category|table\\s+\\.subtype|td\\s+small|td\\s+strong" packages/web/src/styles` found one scoped table descendant selector, `packages/web/src/styles/router/a2a-contract.css:52:.af-a2a-table td small`. The rule at `packages/web/src/styles/router/a2a-contract.css:51-58` styles all `small` descendants under table cells, adjacent to the documented pitfall at `docs/visualization/design-system.md:280-282,290-297`.
- Adjacent broad list/span selectors are not table `td span`, but still match the documented "narrow descendants with direct children" guidance: `.graph-schema-property span` at `packages/web/src/styles/features/graph.css:871-876`, `.af-review-highlight-entry span` at `packages/web/src/styles/router/comments.css:82-96`, and `.af-path-candidate span` at `packages/web/src/styles/router/design.css:785-788`. HYPOTHESIS: these are not breaking `CategoryBadge` today because the current markup does not nest badges there, but they are future badge breakpoints.

## Simplifications

- Choose one fate for the right inspector before touching adjacent code. Delete-permanent yields the clearest LOC reduction and removes the parked runtime editor; revive keeps the contract-editing path but should remove the false flag and the no-inspector branch. Evidence and LOC are in the first section.
- If runtime contract editing must stay, move/reuse it in the active bottom runtime tab rather than keeping it hidden behind `INSPECTOR_ENABLED=false`: bottom runtime currently stops at `RuntimeContractSidebar` (`packages/web/src/routes/design/DesignBottomPanel.tsx:131-133`), while the editor is hidden in the right pane (`packages/web/src/routes/design/DesignGraphPanel.tsx:178-179` and `packages/web/src/design/RuntimeContractPanel.tsx:88-277`).
- Delete the never-active editable `invoke_binding` select path if no product decision revives runtime-control editing. The current behavior is already read-only because `isNodeRuntimeControlEditable` always returns false (`packages/web/src/components/graphElementEditorModel.ts:57-59`, `packages/web/src/components/GraphElementEditor.tsx:570-590`).
- Extract a single `graphNodeKindToModuleCategory` helper used by inspector, node cards, and editor. Current duplicate definitions are at `packages/web/src/components/GraphInspector.tsx:32-40`, `packages/web/src/components/graph/nodeTypes.tsx:6-14`, and `packages/web/src/components/GraphElementEditor.tsx:897-903`.
- Extract shared graph tabs from the duplicate `GraphElementTabs` implementations at `packages/web/src/components/GraphInspector.tsx:100-125` and `packages/web/src/components/GraphElementEditor.tsx:185-210`.
- Centralize optional edge-ID fallback into one helper used by `DesignWorkbench`, `GraphCanvas`, `GraphElementEditor`, and `layout` (`packages/web/src/routes/DesignWorkbench.tsx:112-114`, `packages/web/src/components/GraphCanvas.tsx:427-431,1118-1120`, `packages/web/src/components/GraphElementEditor.tsx:998-1000`, `packages/web/src/components/graph/layout.ts:282-288`).
- Split `GraphCanvas` by responsibility: keep the visual shell, move edit-graph pure helpers (`packages/web/src/components/GraphCanvas.tsx:879-1120`) and collaboration marks (`packages/web/src/components/GraphCanvas.tsx:1122-1221`) into smaller tested modules.
- Split `GraphElementEditor` into node form, edge form, reusable JSON mapping/list helpers, and tabs. Current monolith boundaries are visible at `packages/web/src/components/GraphElementEditor.tsx:366-590` for node runtime fields and `packages/web/src/components/GraphElementEditor.tsx:905-991` for parsers.
- Split `A2AContractPanel` only if it remains in active graph review scope. It is live through bottom tabs (`packages/web/src/routes/design/DesignA2ATab.tsx:48-56`) and currently contains editor section utilities through `packages/web/src/design/A2AContractPanel.tsx:522-810`.
- Replace raw color fallbacks and broad descendant selectors as cleanup-only PRs: raw color evidence is at `packages/web/src/styles/category.css:86-96,99-117,197-212`, `packages/web/src/components/GraphCanvas.tsx:1133-1139`, and `packages/web/src/components/graph/edgeTypes.tsx:13-97,141-161`; broad descendant evidence is at `packages/web/src/styles/router/a2a-contract.css:51-58`, `packages/web/src/styles/features/graph.css:871-876`, `packages/web/src/styles/router/comments.css:82-96`, and `packages/web/src/styles/router/design.css:785-788`.

## Ranked action list (top 10)

1. Decide `INSPECTOR_ENABLED` fate. Delete-permanent removes about 300 LOC lower-bound and deletes the parked runtime editor/selection-header/right-pane plumbing; revive removes only about 10-15 LOC of flag/no-inspector plumbing and makes the existing right pane visible again. Evidence: `packages/web/src/routes/design/designStageModelCore.ts:8`, `packages/web/src/routes/design/DesignGraphPanel.tsx:112,176-207`, LOC commands in the first section.
2. If keeping runtime contract editing, relocate or revive it deliberately. The live bottom runtime tab renders only `RuntimeContractSidebar` at `packages/web/src/routes/design/DesignBottomPanel.tsx:131-133`, while the actual editor is reachable only through the disabled right pane at `packages/web/src/routes/design/DesignGraphPanel.tsx:178-179` and `packages/web/src/design/RuntimeContractPanel.tsx:88-277`.
3. Delete or revive the never-active node runtime-control editor. `isNodeRuntimeControlEditable` is hard false at `packages/web/src/components/graphElementEditorModel.ts:57-59`, while the hidden select path and labels remain at `packages/web/src/components/GraphElementEditor.tsx:71-83,570-586`.
4. Fix node-card subtype visual-owner drift. `nodeTypes.tsx` renders arbitrary `execution_kind` as `SubtypeBadge` at `packages/web/src/components/graph/nodeTypes.tsx:50-65`, contradicting the execution-kind de-duplication guidance at `docs/visualization/design-system.md:240-242` when fixtures contain values like `llm_agent` and `request_input` (`templates/regression-scenarios/scenario-d-graph-workflow/analysis-result.json:545-577`).
5. Extract graph-kind/category mapping. Three duplicate implementations exist at `packages/web/src/components/GraphInspector.tsx:32-40`, `packages/web/src/components/graph/nodeTypes.tsx:6-14`, and `packages/web/src/components/GraphElementEditor.tsx:897-903`.
6. Extract shared graph tabs. Duplicate tab components exist at `packages/web/src/components/GraphInspector.tsx:100-125` and `packages/web/src/components/GraphElementEditor.tsx:185-210`.
7. Centralize edge-ID fallback. Current fallbacks appear in `packages/web/src/routes/DesignWorkbench.tsx:112-114`, `packages/web/src/components/GraphCanvas.tsx:427-431,1118-1120`, `packages/web/src/components/GraphElementEditor.tsx:998-1000`, and `packages/web/src/components/graph/layout.ts:282-288`.
8. Split `GraphCanvas` before adding more graph UI behavior. It is 1131 nonblank/non-comment LOC by command evidence and mixes shell, edit mode, ReactFlow stage, mutation helpers, and collaboration marks across `packages/web/src/components/GraphCanvas.tsx:40-1221`.
9. Split `GraphElementEditor` and `A2AContractPanel` along existing seams. `GraphElementEditor` is 916 nonblank/non-comment LOC and mixes node/edge/forms/parsers (`packages/web/src/components/GraphElementEditor.tsx:366-1001`); `A2AContractPanel` is 769 and carries live A2A inspector/editor/section helpers (`packages/web/src/design/A2AContractPanel.tsx:99-810`).
10. Clean design-system drift in a separate style-only pass: document/code mismatch for input/output category glyphs (`docs/visualization/design-system.md:129-135` vs `packages/web/src/components/CategoryBadge.tsx:23-28`), missing runtime subtype docs (`packages/web/src/analyzer/types.ts:101-108`, `packages/web/src/components/CategoryBadge.tsx:21,41-46`), raw token fallbacks (`packages/web/src/styles/category.css:86-96,99-117,197-212`; `packages/web/src/components/graph/edgeTypes.tsx:13-97`), and the table descendant selector at `packages/web/src/styles/router/a2a-contract.css:51-58`.
