# Companion Graph domain

Canonical strict Graph IR types, validation, semantic revisions, typed edit
operations, and element-level diffs. This package has no HTTP, filesystem, MCP,
or React dependency.
