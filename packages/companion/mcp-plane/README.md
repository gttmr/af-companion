# Companion MCP plane

Two project-scoped Tools expose the latest Graph workspace and revision-checked
Graph edit operations. The server hot-reads a mode-0600 capability file and may
connect only to its loopback Graph Control origin.
