node scripts/validate-skills.mjs
node -e 'const fs=require("node:fs");const p="templates/skill-scenarios/S03-agent-selected-mcp/prompt.md";const expected="문서 검토 Agent가 필요할 때만 OCR 텍스트 추출 도구를 호출해서 서류를 검토하게 하고 싶어. OCR 기능은 외부 MCP 서버로 제공돼. Agent Factory 기준으로 구조를 잡아줘. 파일은 만들지 말고 설계 설명만 해줘.";if(fs.readFileSync(p,"utf8").trimEnd()!==expected)process.exit(1)'
node -e 'JSON.parse(require("node:fs").readFileSync("templates/skill-scenarios/S03-agent-selected-mcp/context/reviewed-candidates.json","utf8"))'
node -e 'JSON.parse(require("node:fs").readFileSync("templates/skill-scenarios/S03-agent-selected-mcp/expected-skill.json","utf8"))'
