# Expected Structure and Behavior

- 파일을 쓰지 않고 discovery 결과를 응답으로만 제시한다.
- 상담 유형 분류 책임을 가진 단일 Agent 후보와 그 근거를 식별한다.
- 입력, 출력, 위험, 확인할 정보와 가정을 분리한다.
- 실행 흐름 소유 근거가 없으므로 별도 Workflow를 만들지 않는다.
- 독립 호출 계약 근거가 없으므로 Tool 후보를 억지로 추가하지 않는다.
