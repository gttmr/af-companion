# Selected skills — condition (b) af-only

Explicit load instruction in the preamble named exactly two files:

1. `/home/ilmaswsl/work/af-companion-skillsync/.agents/skills/_shared/adk/graph-and-dynamic-workflows.md`
2. `/home/ilmaswsl/work/af-companion-skillsync/.agents/skills/_shared/adk/agents-workflows-tools.md`

- No upstream (`google-agents-cli-*`) reference was named or, as far as the captured output shows, consulted.
- Direct proof that the files were opened is **unverified** (only `tail -120` of the transcript was captured). Strong indirect evidence: the produced code adopts the async-generator routing form asserted only by `graph-and-dynamic-workflows.md:100`, which condition (a) did not produce.
