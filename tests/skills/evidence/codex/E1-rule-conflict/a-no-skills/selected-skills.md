# Selected skills — condition (a) no-skills

- Explicitly loaded card files: **none**. No preamble was prepended; the prompt was the bare probe.
- Auto-selected skills: none applicable — `codex exec` has no skill auto-loader in this configuration.
- Model's own grounding (from its final message): it cited the adk-python v2.4.0 GitHub release, the raw `llm_agent.py` at tag v2.4.0, and `https://adk.dev/workflows/graph-routes/`. Whether those fetches actually succeeded is **unverified** (only the tail of the transcript was captured).
