# Codex Scenario Evidence

Codex fresh-session run은 `<scenario-id>/<run-id>/` 아래에 `tests/skills/README.md`가 정한 일곱 evidence 파일만 저장한다. expected answer, rubric, secret, private endpoint, raw customer data는 저장하지 않는다.

