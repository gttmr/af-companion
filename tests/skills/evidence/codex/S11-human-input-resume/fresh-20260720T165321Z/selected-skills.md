# Selected Skills and references

1. `af-workflow` was read first even though its own Not Trigger section sends an explicit approved-scaffold request directly to `af-scaffold-runtime`.
2. `af-scaffold-runtime` was then selected as the implementation Skill and used for the approved standalone generation path.

This repeats the direct-routing deviation from the prior S11 run. The captured command stream did not retain enough untruncated evidence to certify every mandatory direct reference read, so progressive-disclosure completeness remains unverified rather than passed.

No retired Skill ID or `_shared` directory was selected as a Skill.
