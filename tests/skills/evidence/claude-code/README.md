# Claude Code Scenario Evidence

Claude Code fresh-session run은 `<scenario-id>/<run-id>/` 아래에 `tests/skills/README.md`가 정한 일곱 evidence 파일만 저장한다. `.agents/skills`가 자동 발견되지 않으면 test-only explicit-load 사실과 경로를 `selected-skills.md`에 기록한다.

