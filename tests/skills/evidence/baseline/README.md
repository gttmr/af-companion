# Baseline evidence (legacy 4-skill system)

- Date: 2026-07-18 · Commit: 7deea45 · Runner: codex companion, model `gpt-5.6-luna`, effort `low`, read-only, fresh thread per run
- 대상: 구 스킬 체계(af-analyze-requirement 등 4종)가 아직 canonical일 때 대표 시나리오 3건(S01, S03, S13)
- **중요 한계**: 이 시점의 워킹트리에는 문서 vNext(새 taxonomy/graph-ir/operating-model)가 이미 존재한다. 따라서 이 baseline은 "구 스킬 + 신 문서" 조합이며 순수 pre-vNext 기준선이 아니다(구 문서로의 롤백은 사용자 워킹트리 보호 원칙상 수행하지 않음).
- read-only 실행이므로 파일 생성 행동은 관찰 범위 밖. 스킬 선택·분류 어휘·구조 판단·게이트 준수를 응답 텍스트로 평가.
